I need to add a new "Game Section" to my Home Page (`src/pages/Home.tsx`).

### 1. Placement (Crucial)
Insert this new section **immediately after** the Hero Section (the 3D Robot) and **before** the Menu Section.

### 2. Design Concept: "The Waiting Lounge"
Create a section that invites users to play while they wait for their order.
- **Headline:** "WHILE YOU WAIT..." (Titan One font, large).
- **Sub-headline:** "Challenge yourself. Beat the high score before your coffee arrives."
- **Theme:** Dark, immersive, slightly cyberpunk/neon to match the game URL.

### 3. Implementation Details
Create a container that looks like a futuristic arcade screen.
- **Container:** `w-full`, `h-[80vh]` (make it tall), `rounded-3xl`, `overflow-hidden`.
- **Border:** Add a `border border-white/20` and a `shadow-2xl` glow effect (indigo/purple).
- **The Game Embed:**
  - Use an `iframe` pointing to: `https://play.garance.com/`
  - Attributes: `width="100%"`, `height="100%"`, `allow="autoplay; fullscreen"`.
  - **Fallback/Overlay:** Since some external sites block iframes, add a button on top that says "Launch Game in Fullscreen" if it doesn't load, or make the iframe container clickable to open in a new tab for a better experience.

### 4. Code Generation
Please provide the updated code for the `Home.tsx` component with this new `<section id="game">` added in the correct spot.

**Styling Suggestion:**
```tsx
<section id="game" className="py-24 px-6 bg-black relative z-20">
  <div className="container mx-auto">
    <div className="text-center mb-12">
      <h2 className="text-4xl md:text-6xl font-black text-white mb-4">WHILE IT BREWS</h2>
      <p className="text-gray-400">Kill time, not vibes. Play now.</p>
    </div>
    
    <div className="relative w-full h-[700px] rounded-[2rem] border border-white/10 shadow-[0_0_50px_rgba(79,70,229,0.3)] overflow-hidden bg-slate-900">
      <iframe 
        src="[https://play.garance.com/](https://play.garance.com/)" 
        className="w-full h-full object-cover"
        title="Game"
        allowFullScreen
      />
      {/* Optional Overlay if needed */}
      <div className="absolute bottom-6 right-6 pointer-events-none">
        <span className="bg-black/50 backdrop-blur-md px-4 py-2 rounded-full text-xs text-white/50 border border-white/10">Interactive Experience</span>
      </div>
    </div>
  </div>
</section>